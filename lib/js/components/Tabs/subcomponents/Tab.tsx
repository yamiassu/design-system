// Component
import { TabPropsInterface } from "../props"

export default function Tab (props: TabPropsInterface) {
	// -------------------------------------------------
	// Render
	// -------------------------------------------------

	return props.children
}